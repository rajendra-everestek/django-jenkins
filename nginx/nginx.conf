server {
    listen  0.0.0.0:8000;
    server_name 44.195.25.178;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/ubuntu/django-jenkins/djangojenkin;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}